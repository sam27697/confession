.claude/worktrees/vigorous-feistel-639f72 | A real directory path, named in commit b4ec45f because that worktree is where the recovered PGlite harness fix had been stranded. The scan's model-name pattern matches the leading directory name, which here is a path on disk and not an authorship tell. The directory was deleted in the same cleanup; the path is kept in the message so the recovery is traceable.
