# Task B3-T03
TASK_ID: B3-T03
TITLE: Atmospheric dark-mode vignette framing with citron ambient wash on empty states on /inbox and /sent
ROOT_CAUSE: RC-B303
ALLOWED_PATHS:
- app/inbox/page.tsx
- app/sent/page.tsx
- app/globals.css

## Criteria
- AC1 (NEW): Empty state card containers (.inbox-empty, .sent-empty, or .card--empty) render subtle radial vignette framing with citron-wash token
- AC2 (NEW): Empty state border hairline uses codified token (--line or --line-strong) with smooth card radius
- AC3 (KEEP): Daily spark banner, sub-navigation tabs with volume badges, and empty-state action links strictly preserved
- AC4 (KEEP): Sent filter pills and empty status copy strictly preserved
- HC-35 (HUMAN): Atmospheric comfort and OLED depth perception in empty states

## Expected metric
Spatial depth and atmospheric welcoming feel on empty screens.

## Risk
Vignette clashing with card background (mitigated: soft transparent radial gradient).

## Dependencies
none

## Rollback
git revert
