The base panel: warm charcoal surface, hairline border, 22px radius.

```jsx
<Card>…</Card>
<Card bubble>نص الرسالة</Card>
<Card tone="rose" pad="lg">…</Card>
```

`bubble` cuts the bottom-leading corner to 6px — the brand's signature shape, used for anything that is somebody's words. Reserve `tone="rose"` for the reveal exchange.
